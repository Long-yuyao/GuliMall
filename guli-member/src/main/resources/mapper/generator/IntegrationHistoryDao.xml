<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="gulimember.dao.IntegrationHistoryDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="gulimember.entity.IntegrationHistoryEntity" id="integrationHistoryMap">
        <result property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="createTime" column="create_time"/>
        <result property="count" column="count"/>
        <result property="remark" column="remark"/>
        <result property="sourceType" column="source_type"/>
    </resultMap>

	<select id="queryObject" resultType="gulimember.entity.IntegrationHistoryEntity">
		select * from ums_integration_history where id = #{value}
	</select>

	<select id="queryList" resultType="gulimember.entity.IntegrationHistoryEntity">
		select * from ums_integration_history
        <choose>
            <when test="sidx != null and sidx.trim() != ''">
                order by ${sidx} ${order}
            </when>
			<otherwise>
                order by id desc
			</otherwise>
        </choose>
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>
	
 	<select id="queryTotal" resultType="int">
		select count(*) from ums_integration_history 
	</select>
	 
	<insert id="save" parameterType="gulimember.entity.IntegrationHistoryEntity" useGeneratedKeys="true" keyProperty="id">
		insert into ums_integration_history
		(
			`user_id`, 
			`create_time`, 
			`count`, 
			`remark`, 
			`source_type`
		)
		values
		(
			#{userId}, 
			#{createTime}, 
			#{count}, 
			#{remark}, 
			#{sourceType}
		)
	</insert>
	 
	<update id="updatee" parameterType="gulimember.entity.IntegrationHistoryEntity">
		update ums_integration_history 
		<set>
			<if test="userId != null">`user_id` = #{userId}, </if>
			<if test="createTime != null">`create_time` = #{createTime}, </if>
			<if test="count != null">`count` = #{count}, </if>
			<if test="remark != null">`remark` = #{remark}, </if>
			<if test="sourceType != null">`source_type` = #{sourceType}</if>
		</set>
		where id = #{id}
	</update>
	
	<delete id="deletee">
		delete from ums_integration_history where id = #{value}
	</delete>
	
	<delete id="deleteBatch">
		delete from ums_integration_history where id in 
		<foreach item="id" collection="array" open="(" separator="," close=")">
			#{id}
		</foreach>
	</delete>

</mapper>