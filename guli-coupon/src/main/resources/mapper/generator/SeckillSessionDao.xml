<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="gulicoupon.dao.SeckillSessionDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="gulicoupon.entity.SeckillSessionEntity" id="seckillSessionMap">
        <result property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="startTime" column="start_time"/>
        <result property="endTime" column="end_time"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

	<select id="queryObject" resultType="gulicoupon.entity.SeckillSessionEntity">
		select * from sms_seckill_session where id = #{value}
	</select>

	<select id="queryList" resultType="gulicoupon.entity.SeckillSessionEntity">
		select * from sms_seckill_session
        <choose>
            <when test="sidx != null and sidx.trim() != ''">
                order by ${sidx} ${order}
            </when>
			<otherwise>
                order by id desc
			</otherwise>
        </choose>
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>
	
 	<select id="queryTotal" resultType="int">
		select count(*) from sms_seckill_session 
	</select>
	 
	<insert id="save" parameterType="gulicoupon.entity.SeckillSessionEntity" useGeneratedKeys="true" keyProperty="id">
		insert into sms_seckill_session
		(
			`name`, 
			`start_time`, 
			`end_time`, 
			`status`, 
			`create_time`
		)
		values
		(
			#{name}, 
			#{startTime}, 
			#{endTime}, 
			#{status}, 
			#{createTime}
		)
	</insert>
	 
	<update id="coupdate" parameterType="gulicoupon.entity.SeckillSessionEntity">
		update sms_seckill_session 
		<set>
			<if test="name != null">`name` = #{name}, </if>
			<if test="startTime != null">`start_time` = #{startTime}, </if>
			<if test="endTime != null">`end_time` = #{endTime}, </if>
			<if test="status != null">`status` = #{status}, </if>
			<if test="createTime != null">`create_time` = #{createTime}</if>
		</set>
		where id = #{id}
	</update>
	
	<delete id="codelete">
		delete from sms_seckill_session where id = #{value}
	</delete>
	
	<delete id="deleteBatch">
		delete from sms_seckill_session where id in 
		<foreach item="id" collection="array" open="(" separator="," close=")">
			#{id}
		</foreach>
	</delete>

</mapper>