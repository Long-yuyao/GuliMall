<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yuyao.guliproduct.dao.CommentReplayDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.yuyao.guliproduct.entity.CommentReplayEntity" id="commentReplayMap">
        <result property="id" column="id"/>
        <result property="commentId" column="comment_id"/>
        <result property="replyId" column="reply_id"/>
    </resultMap>

	<select id="queryObject" resultType="com.yuyao.guliproduct.entity.CommentReplayEntity">
		select * from pms_comment_replay where id = #{value}
	</select>

	<select id="queryList" resultType="com.yuyao.guliproduct.entity.CommentReplayEntity">
		select * from pms_comment_replay
        <choose>
            <when test="sidx != null and sidx.trim() != ''">
                order by ${sidx} ${order}
            </when>
			<otherwise>
                order by id desc
			</otherwise>
        </choose>
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>
	
 	<select id="queryTotal" resultType="int">
		select count(*) from pms_comment_replay 
	</select>
	 
	<insert id="save" parameterType="com.yuyao.guliproduct.entity.CommentReplayEntity" useGeneratedKeys="true" keyProperty="id">
		insert into pms_comment_replay
		(
			`comment_id`, 
			`reply_id`
		)
		values
		(
			#{commentId}, 
			#{replyId}
		)
	</insert>
	 
	<update id="proupdate" parameterType="com.yuyao.guliproduct.entity.CommentReplayEntity">
		update pms_comment_replay 
		<set>
			<if test="commentId != null">`comment_id` = #{commentId}, </if>
			<if test="replyId != null">`reply_id` = #{replyId}</if>
		</set>
		where id = #{id}
	</update>
	
	<delete id="prodelete">
		delete from pms_comment_replay where id = #{value}
	</delete>
	
	<delete id="deleteBatch">
		delete from pms_comment_replay where id in 
		<foreach item="id" collection="array" open="(" separator="," close=")">
			#{id}
		</foreach>
	</delete>

</mapper>