<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yuyao.guliproduct.dao.SpuDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.yuyao.guliproduct.entity.SpuEntity" id="spuMap">
        <result property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="categoryId" column="category_id"/>
        <result property="brandId" column="brand_id"/>
        <result property="publishStatus" column="publish_status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

	<select id="queryObject" resultType="com.yuyao.guliproduct.entity.SpuEntity">
		select * from pms_spu where id = #{value}
	</select>

	<select id="queryList" resultType="com.yuyao.guliproduct.entity.SpuEntity">
		select * from pms_spu
        <choose>
            <when test="sidx != null and sidx.trim() != ''">
                order by ${sidx} ${order}
            </when>
			<otherwise>
                order by id desc
			</otherwise>
        </choose>
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>
	
 	<select id="queryTotal" resultType="int">
		select count(*) from pms_spu 
	</select>
	 
	<insert id="save" parameterType="com.yuyao.guliproduct.entity.SpuEntity" useGeneratedKeys="true" keyProperty="id">
		insert into pms_spu
		(
			`name`, 
			`category_id`, 
			`brand_id`, 
			`publish_status`, 
			`create_time`, 
			`update_time`
		)
		values
		(
			#{name}, 
			#{categoryId}, 
			#{brandId}, 
			#{publishStatus}, 
			#{createTime}, 
			#{updateTime}
		)
	</insert>
	 
	<update id="proupdate" parameterType="com.yuyao.guliproduct.entity.SpuEntity">
		update pms_spu 
		<set>
			<if test="name != null">`name` = #{name}, </if>
			<if test="categoryId != null">`category_id` = #{categoryId}, </if>
			<if test="brandId != null">`brand_id` = #{brandId}, </if>
			<if test="publishStatus != null">`publish_status` = #{publishStatus}, </if>
			<if test="createTime != null">`create_time` = #{createTime}, </if>
			<if test="updateTime != null">`update_time` = #{updateTime}</if>
		</set>
		where id = #{id}
	</update>
	
	<delete id="prodelete">
		delete from pms_spu where id = #{value}
	</delete>
	
	<delete id="deleteBatch">
		delete from pms_spu where id in 
		<foreach item="id" collection="array" open="(" separator="," close=")">
			#{id}
		</foreach>
	</delete>

</mapper>